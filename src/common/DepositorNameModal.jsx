// src/components/common/DepositorNameModal.jsx

import React, { useState, useEffect } from 'react';
import { theme, styles } from '../../styles';

const DepositorNameModal = ({ isOpen, onClose, onSubmit, customerName }) => {
    // ... (이전에 제공한 DepositorNameModal 코드 전체)
    // ...
};

export default DepositorNameModal;