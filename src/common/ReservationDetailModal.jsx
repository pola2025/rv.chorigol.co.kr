// src/components/common/ReservationDetailModal.jsx

import React from 'react';
import { useWindowWidth } from '../../utils';
import { theme, styles } from '../../styles';

const ReservationDetailModal = ({ isOpen, onClose, reservation, onConfirm, onCancelReservation }) => {
    // ... (이전에 제공한 ReservationDetailModal 코드 전체)
    // ...
};

export default ReservationDetailModal;